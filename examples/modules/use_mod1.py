import reusable  # find reusable.py and execute the code

print(reusable.square(10))
print(reusable.cube(9))
x = reusable.square(1000)
print(x)
print(reusable.COLORS)

print(dir(reusable))
print()

print(dir(__builtins__))
